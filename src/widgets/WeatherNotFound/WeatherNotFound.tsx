import { StyleSheet, Text } from 'react-native'
import ScreenLayout from '../ScreenLayout/ScreenLayout'

export default function WeatherNotFound() {
  return (
    <ScreenLayout>
      <Text>WeatherNotFound</Text>
    </ScreenLayout>
  )
}

const styles = StyleSheet.create({})
